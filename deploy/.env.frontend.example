# Frontend Environment Variables Template
# These are baked into the Docker image at build time

# ============================================================================
# FIREBASE CONFIGURATION (Public - Safe to Commit)
# ============================================================================
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSyAJ0RDWrBYF6yIDvwdyoAVO4K5QkL218Yc
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=hushh-pda.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=hushh-pda
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=hushh-pda.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=1006304528804
NEXT_PUBLIC_FIREBASE_APP_ID=1:1006304528804:web:default

# ============================================================================
# API CONFIGURATION
# ============================================================================

# Backend URL (Python FastAPI) — used by lib/api/consent.ts, lib/config.ts, api routes
# UPDATE with actual backend URL after backend deployment
NEXT_PUBLIC_BACKEND_URL=https://consent-protocol-1006304528804.us-central1.run.app

# ============================================================================
# BUILD CONFIGURATION
# ============================================================================
CAPACITOR_BUILD=false
ENVIRONMENT_MODE=production

# ============================================================================
# NOTES
# ============================================================================
# 1. These variables are baked into the static build at Docker build time
# 2. NEXT_PUBLIC_* variables are exposed to the browser
# 3. Never include secrets here - they will be visible in the browser
# 4. Firebase config is public and safe to commit
# 5. Update NEXT_PUBLIC_BACKEND_URL with actual backend URL before building

# ==============================================================================
# Cloud Deployment Environment Variables Template
# ==============================================================================
# Copy this file and set the values for your production deployment
# For Cloud Run: These should be set as environment variables or secrets

# ==============================================================================
# FRONTEND (hushh-webapp) - Next.js
# ==============================================================================

# Backend URL - Point to your Python API Cloud Run service
BACKEND_URL=https://your-backend-service.run.app
NEXT_PUBLIC_BACKEND_URL=https://your-backend-service.run.app

# Firebase Configuration (get from Firebase Console)
NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id

# Database URL (Cloud SQL)
# For Cloud Run: use Unix socket path
# DATABASE_URL=postgresql://user:password@/database?host=/cloudsql/project:region:instance
DATABASE_URL=postgresql://hushh_app:password@/hushh_vault?host=/cloudsql/hushh-pda:us-central1:hushh-vault-db

# ==============================================================================
# BACKEND (consent-protocol) - Python FastAPI
# ==============================================================================

# Frontend URL - For CORS (allow requests from your Next.js frontend)
FRONTEND_URL=https://your-frontend-service.run.app

# Root path for Swagger docs - Set to your backend URL so Swagger shows correct endpoints
ROOT_PATH=https://your-backend-service.run.app

# Secret key for signing consent tokens (generate a secure random string)
SECRET_KEY=your-secure-secret-key-here

# Token expiry (milliseconds) - Default: 7 days
DEFAULT_CONSENT_TOKEN_EXPIRY_MS=604800000

# ==============================================================================
# OPTIONAL: Development Overrides
# ==============================================================================

# For local development with Docker Compose:
# BACKEND_URL=http://backend:8000
# FRONTEND_URL=http://frontend:3000

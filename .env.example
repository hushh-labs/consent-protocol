# ============================================================================
# Hushh PDA - Root Environment Configuration
# ============================================================================
# This file documents all environment variables needed for local development.
# Copy to .env and fill in your values.

# ============================================================================
# GCP / Cloud SQL Configuration
# ============================================================================
# Project ID for GCP resources
GCP_PROJECT_ID=hushh-pda

# Cloud SQL Instance (for cloud_sql_proxy)
CLOUD_SQL_INSTANCE=hushh-pda:us-central1:hushh-vault-db

# Database port (default: 5432)
DB_PORT=5432

# ============================================================================
# Cloud SQL Proxy Setup
# ============================================================================
# The Cloud SQL Proxy creates a local tunnel to GCP Cloud SQL.
# Authentication options (choose one):
#
# OPTION 1: Use your GCP user credentials (recommended for local dev)
#   Run: gcloud auth application-default login
#   Then: ./cloud-sql-proxy.exe $CLOUD_SQL_INSTANCE --port 5432
#
# OPTION 2: Use a service account with Cloud SQL Client role
#   ./cloud-sql-proxy.exe $CLOUD_SQL_INSTANCE --credentials-file=cloudsql-service-account.json --port 5432
#
# NOTE: The service-account.json in this repo is for Firebase Admin SDK,
#       NOT Cloud SQL. It does not have cloudsql.instances.get permission.

# ============================================================================
# Service Account Files
# ============================================================================
# service-account.json        - Firebase Admin SDK (for server-side auth)
# cloudsql-service-account.json - Cloud SQL Client (optional, for CI/CD)

# ============================================================================
# Quick Start Commands
# ============================================================================
# 1. Start Cloud SQL Proxy:
#    gcloud auth application-default login
#    ./cloud-sql-proxy.exe hushh-pda:us-central1:hushh-vault-db --port 5432
#
# 2. Start Python Backend:
#    cd consent-protocol && python server.py
#
# 3. Start Next.js Frontend:
#    cd hushh-webapp && npm run dev
#
# 4. Open browser: http://localhost:3000
